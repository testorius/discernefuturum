---
import type { SEOProps } from '../seo/types';
import SEO from '../components/shared/SEO.astro';
import Navigation from '../components/shared/Navigation.astro';
import Footer from '../components/shared/Footer.astro';

// Extend SEOProps to include required layout props
interface Props extends Omit<SEOProps, 'title' | 'description'> {
  title: string;  // Make title required
  description: string;  // Make description required
  siteName?: string;  // Optional with default
}

const { 
  title, 
  description, 
  siteName = "Alexander Paul",
  ...seoProps
} = Astro.props;

// Define font families for better maintainability
const fontFamilies = [
  'Bebas+Neue',
  'Bona+Nova+SC:ital,wght@0,400;0,700;1,400',
  'Domine:wght@400..700',
  'Outfit:wght@100..900',
  'Oxanium:wght@200..800',
  'Radley:ital@0;1'
].join('&family=');

const googleFontsUrl = `https://fonts.googleapis.com/css2?family=${fontFamilies}&display=swap`;
---

<!DOCTYPE html>
<html lang="de">
<!-- 
👋👋👋 Thanks for visiting! 
Nope, this wasn't built using WordPress, Wix or PowerPoint. 
Just saving you some time 💐 I used AI to code all components for astro.js and did some manual refinements.
Let's have a coffee or kombucha to Chat about our next masterplan 🤓👉 https://app.reclaim.ai/m/alexanderpaul/coffee
Cheers! Alex
-->
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" href="/discernefuturum/images/alexanderpaullogo.svg" type="image/svg+xml">
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <!-- ... rest of your meta tags ... -->
  </head>
  <body class="min-h-screen bg-gray-50 flex flex-col">
    <Navigation />
    <main class="pt-16 flex-grow">
      <slot />
    </main>
    <Footer />
  </body>
</html>